<Window x:Class="UllnnovationHub.ToolKIt.UI.UMessageBox" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:chrome="http://schemas.microsoft.com/winfx/2006/xaml/presentation" Title="MessageBoxWindow" Width="800" Height="450" MinWidth="360" MinHeight="200" MaxWidth="400" MaxHeight="300" MouseLeftButtonDown="Window_MouseLeftButtonDown" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <WindowChrome.WindowChrome>
        <chrome:WindowChrome GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>
    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Background="#448EC9">
                <Border CornerRadius="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="tB_Title" Margin="10,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="20" Foreground="White" />
                        <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                            <Button x:Name="btn_Close" Margin="0,0,10,0" Background="Transparent" BorderThickness="0" Click="btn_Close_Click" Content="x" FontSize="25" Foreground="black" />
                        </StackPanel>
                    </Grid>
                </Border>
            </DockPanel>
            <Grid Grid.Row="1" Margin="30" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="Content" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" Foreground="Black" TextWrapping="Wrap" />
            </Grid>

            <StackPanel Grid.Row="2" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                <Button x:Name="Confirm" Width="100" Height="30" Margin="5,0" Click="Confirm_Click" Content="确定" IsDefault="True" />
                <Button x:Name="No" Width="100" Height="30" Margin="5,0" Click="No_Click" Content="否" Visibility="Collapsed" />
                <Button x:Name="Cancel" Width="100" Height="30" Margin="5,0" Click="Cancel_Click" Content="取消" IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>

</Window> 